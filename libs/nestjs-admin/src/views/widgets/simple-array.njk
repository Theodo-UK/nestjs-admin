{% extends 'widgets/base-widget.njk' %}

{% block input %}
  {% macro field(name, value = '') %}
    <div>
      <input
        id="{{ name }}"
        name="{{ name }}"
        value="{{ value }}"
      />
      <button type="button">X</button>
    </div>
  {% endmacro %}

  {% for value in column.getEntityValue(entity) %}
    {% set fieldName = column.propertyName + "["+ loop.index0+ "]" %}
    {{ field(fieldName, value) }}
  {% endfor %}

  {% set fieldName = column.propertyName + "["+ "3" + "]" %}
  {# <button type="button" onclick="this.parentElement.append('{{field(fieldName)|forceescape}}')">+</button> #}
{% endblock %}
