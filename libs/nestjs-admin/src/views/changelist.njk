<html>
<body>
  <h1>{{ metadata.name }}</h1>
  <h2>{{ count }} results</h2>
  <ul>
    {% for entity in entities %}
      <li>
        <a href="{{ 'change' | adminUrl(section, metadata, entity) }}">
          {{ entity | displayName(metadata) }}
        </a>
      </li>
    {% endfor %}
  </ul>
</body>
</html>
