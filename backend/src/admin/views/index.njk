<html>
<body>
  <h1>Administration</h1>
  <ul>
    {% for section in sections %}
      <li>
        <h2>{{ section.name }}</h2>
        <ul>
          {% for entityMD in section.getEntitiesMetadata() %}
            <li>{{ entityMD.name }}</li>
          {% endfor %}
        </ul>
      </li>
    {% endfor %}
  </ul>
</body>
</html>
